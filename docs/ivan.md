- [ ] convert to v2
- [ ] how best to make add on UIs, maybe concord becomes a low-code system
- [ ] new flow DSLs from primitives, can Ivan expose his parser
- [ ] how to provide better names to generic tasks like the `command` task
- [ ] can i make a flow stop without throwing an exception, if a CLI command fails, i just want to fail the flow, not throw an exception and emit a stack trace. it's too much noise
  in the UI
