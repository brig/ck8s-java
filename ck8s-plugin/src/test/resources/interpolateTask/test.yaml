name: "${name}"
kind: "/aep/Configuration"

clientId: "${clientId}"
environmentTier: "dev"

aep:
  externalDatabase: false
  test: "test-value"
  cognito:
    uiUserPoolClientName: "${uiUserPoolClientName}"
